Python, using the pathlib class, contains a method that allows it to read a file in the same folder as the project or py file you are working on.
However this can be enhanced by using the files full path name(C directory to the filename) 
** Be aware that Visual Studio or possibly just Python 3.9 have some built in function that give outputs already "formatted" **
input() is a function that requests input from user or the code runner to continue the program. It does halt the program until input received 
